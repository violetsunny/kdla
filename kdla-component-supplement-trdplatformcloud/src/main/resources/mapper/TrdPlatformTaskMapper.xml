<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ennew.iot.gateway.dal.mapper.TrdPlatformTaskMapper">

    <resultMap id="BaseResultMap" type="top.kdla.framework.supplement.trdcloud.entity.TrdPlatformTaskEntity" >
        <result column="id" property="id" />
        <result column="task_code" property="taskCode" />
        <result column="p_code" property="pCode" />
        <result column="task_name" property="taskName" />
        <result column="frequency" property="frequency" />
        <result column="api_id" property="apiId" />
        <result column="product_id" property="productId" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
        <result column="update_user" property="updateUser" />
        <result column="update_time" property="updateTime" />
        <result column="is_delete" property="isDelete" />
        <result column="status" property="status" />
        <result column="remark" property="remark" />
    </resultMap>

    <sql id="Base_Column_List">
        id,
        task_code,
        p_code,
        task_name,
        frequency,
        api_id,
        product_id,
        create_user,
        create_time,
        update_user,
        update_time,
        is_delete,
        status,
        remark
    </sql>

    <select id="queryPage" parameterType="Map" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        trd_platform_task u
        WHERE u.is_delete = 0
        <if test="params.taskName != null and params.taskName !=''">
            AND u.task_name LIKE CONCAT('%',#{params.taskName},'%')
        </if>
        <if test="params.pCode != null and params.pCode !=''">
            AND u.p_code = #{params.pCode}
        </if>
        <if test="params.productId != null and params.productId !=''">
            AND u.product_id = #{params.productId}
        </if>
        <if test="params.taskCode != null and params.taskCode !=''">
            AND u.task_code = #{params.taskCode}
        </if>
        ORDER BY u.create_time desc
    </select>

</mapper>